# Variables de entorno para FitTrack
# Copia este archivo a .env y rellena los valores

# Token de API de NaN.builders (para el coach IA)
NAN_API=tu_token_aqui

# Sync a GitHub (opcional, para backup automático de database.json)
GITHUB_REPO_OWNER=tu_usuario_github
GITHUB_REPO_NAME=tu_repo
